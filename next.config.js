/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote domains here if you host images externally (e.g. GitHub avatars)
    remotePatterns: [],
  },
};

module.exports = nextConfig;
